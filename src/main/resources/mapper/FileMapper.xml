<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.FileMapper">
    
    <resultMap id="BaseResultMap" type="com.blog.entity.FileInfo">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="original_name" property="originalName"/>
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="file_size" property="fileSize"/>
        <result column="file_type" property="fileType"/>
        <result column="category" property="category"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    
    <insert id="insert" parameterType="com.blog.entity.FileInfo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO file (user_id, original_name, file_name, file_path, file_size, file_type, category)
        VALUES (#{userId}, #{originalName}, #{fileName}, #{filePath}, #{fileSize}, #{fileType}, #{category})
    </insert>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM file WHERE id = #{id}
    </select>
    
    <delete id="deleteById">
        DELETE FROM file WHERE id = #{id}
    </delete>
    
    <select id="selectByPage" resultMap="BaseResultMap">
        SELECT * FROM file
        <where>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="category != null and category != ''">AND category = #{category}</if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="countFiles" resultType="java.lang.Long">
        SELECT COUNT(*) FROM file
        <where>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="category != null and category != ''">AND category = #{category}</if>
        </where>
    </select>
    
</mapper>


